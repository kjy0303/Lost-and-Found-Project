import React from 'react';
// 아까 src/screens 폴더에 만들어둔 AI 카메라 화면을 불러옵니다!
import RegisterScreen from '../src/screens/RegisterScreen';

export default function RegisterRoute() {
  return <RegisterScreen />;
}